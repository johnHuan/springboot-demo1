#默认使用配置
spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring?serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: johnjohn
    type: com.alibaba.druid.pool.DruidDataSource
mybatis:
  typeAliasesPackage: com.zte.springbootdemo1.pojo
  mapperLocations: classpath:mapper/*.xml

#公共配置与profiles选择无关


#开发配置
